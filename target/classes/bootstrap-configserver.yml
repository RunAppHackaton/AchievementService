spring:
  application:
    name: achievement-service
  cloud:
    config:
      uri: ${CONFIG_URL} # "localhost" or docker config container name
      profile: ${CONFIG_PROFILE} # "localhost" or "docker"