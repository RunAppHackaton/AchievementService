spring:
  application:
    name: achievement-service
  cloud:
    config:
      uri: ${CONFIG_URL}:8887 # "localhost" or docker config container name
      profile: ${CONFIG_PROFILE} # "localhost" or "docker"